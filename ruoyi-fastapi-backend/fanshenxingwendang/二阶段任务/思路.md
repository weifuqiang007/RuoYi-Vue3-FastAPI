现在一阶段只是完成了关于反身性研究的demo。接下来，我需要在此基础上进行进一步优化：
1 关于rag部分。rag部分目前只是将资料进行输入。但是没有对rag部分进行优化。我不知道如何进行优化。需要怎么做呢？有没有类似之类的资料？
需要包括： rag数据如何进行清洗？ rag都有哪些模式？ rag我还希望进行溯源。我想要对整体进行调整。但是我不知道应该怎么办。你有没有好的方式对我进行帮助？

---

# 二阶段 RAG 优化思路（基于实际代码诊断）

> 本文档基于对 `module_rag/` **实际代码**（非规划文档）的全量诊断，回答你的四个问题：① 数据怎么清洗 ② RAG 有哪些模式 ③ 怎么溯源 ④ 整体怎么调整。
> 所有结论都带**文件路径 + 行号**，可直接对照修改。

## 一、先定位：当前 RAG 效果差，差在哪（带代码证据）

把"我不满意结果"翻译成可定位的工程问题。按"致命 / 质量 / 工程债"三档：

### 🔴 P0 致命问题（直接导致结果错或污染）

| # | 问题 | 证据 | 后果 |
|---|---|---|---|
| 1 | **DOCX 解析器缩进 bug** | `service/parser/docx_parser.py:50` 的 `return blocks` 缩进在 `for element in doc.element.body:` 循环**内部** | 任何 .docx 只解析出**第 1 个** block，后面全部丢失。Word 文档知识库基本等于没建 |
| 2 | **删文档不删 chunk（孤儿数据）** | `service/document_service.py:104-117` `delete_documents` 只软删 `rag_document`，不动 `rag_chunk` | chunk 还在表里、带向量、`del_flag='0'`，继续参与检索 → **已删文档的内容仍被召回** |
| 3 | **关键词检索对中文无效** | `service/retrieval_service.py:62-81` 用 `to_tsvector('simple', ...)` | PG `simple` 配置**不做中文分词**，只能命中字面子串，BM25 这一路几乎不召回 → 你以为的"混合检索"其实只有向量一路在干活 |

### 🟡 P1 质量短板（决定召回准不准）

| # | 问题 | 证据 |
|---|---|---|
| 4 | **没有 rerank（重排）** | 全 `module_rag` grep `rerank` 零命中。向量召回 top-20 + RRF 后直接返回，没有 cross-encoder 精排 |
| 5 | **没有相似度阈值** | `retrieval_service.py` 的 WHERE 没有 `score > 阈值`，库里只要有关 chunk 就一定返回，即便余弦只有 0.1 |
| 6 | **chunk 没有标题上下文** | `document_service.py:170` 只把 `page/type` 塞进 metadata，**不把所属章节标题前缀到 chunk 内容**。检索出来一个孤立段落，模型不知道它在讲哪一章 |
| 7 | **没有清洗/过滤** | `utils/text_cleaner.py` 的 `clean_text`、`is_chinese` **全仓无调用方**；`process_document` 流程里没有"清洗"环节；不过滤短 chunk、不去重、不剔乱码 |
| 8 | **溯源链路在 controller 就断了** | `controller/retrieval_controller.py:60-65` 响应只返回 `chunk_id/doc_id/content[:200]/score`，**丢弃 metadata**；`module_learning` 拼 prompt 时连 `chunk_id` 都不要 → 前端无法显示"这段来自哪份文档第几页" |
| 9 | **prompt 拼接散落 4 处且不一致** | 情境区 `scenario_service.py:308`、决策区 `decision_service.py:212`、反思区 `reflection_service.py:589`、批阅区 `review_service.py:432` 各自拼接，截断长度（300/不截/200）、top_k（5/5/4）、前缀格式都不同，没有统一抽象 |
| 10 | **通用 AI 对话完全没接 RAG** | `module_ai/service/ai_chat_service.py` grep `RetrievalService/rag` 零命中，普通对话不注入知识 |

### ⚪ P2 工程债（量大后才暴露）

- `dao/chunk_dao.py:11-26` `bulk_insert` 是**伪批量**（逐条 `db.add + flush`），大文档很慢
- `sql/ruoyi-fastapi-pg.sql:1339` 用 `ivfflat(lists=10)`，不是 HNSW；lists=10 偏小
- `retrieval_service.py:26` 每次查询多发一条 `SET ivfflat.probes=10`
- `chunk` 表没存 `embedding_model` 版本 → 换模型无法区分新旧向量，只能全量重建
- `document_service.py:71` 上传**同步** await 处理，大 PDF 阻塞上传接口

**一句话诊断**：你的 RAG 是一个"骨架完整、每环都到最简"的 **Naive RAG**。检索效果差，主要是 **P0#3（中文关键词路废了）+ P1#4（没 rerank）+ P1#6（没标题上下文）+ P1#5（没阈值过滤）** 四个叠加的结果，不是单一原因。

---

## 二、RAG 的「模式」全景（你问"都有哪些模式"）

RAG 不是一项技术，是一组可插拔的模块。学术界把它分成三代演进，每一代里又有具体技术模式。先建立地图，再决定走到哪。

### 2.1 三代演进范式

```
Naive RAG（你现在在这里）
  Index: 解析→固定分块→embedding→存
  Query: query→向量召回 top-k→拼 prompt→生成
  问题：召回不准、长尾丢、无法溯源、模型幻觉

        ↓ 在 retrieve 前后加优化

Advanced RAG（下一步目标）
  Pre-retrieval:  query 改写 / 路由 / 分解
  Retrieval:      向量 + 关键词 混合 + RRF
  Post-retrieval: rerank 精排 + 阈值过滤 + 去重 + 压缩

        ↓ 引入反思与模块化

Modular RAG（高级）
  可插拔模块 + Self-RAG/CRAG（模型自己判断要不要检索、检索够不够）+ 多跳检索
```

### 2.2 各阶段可选的技术模式（标★的是你下一步该上的）

| 阶段 | 模式 | 解决什么 | 你的现状 | 建议 |
|---|---|---|---|---|
| **解析** | 纯文本抽取 | 基础 | ✓ PyMuPDF | 保留 |
| | 版面识别（DeepDoc/OCR） | 复杂 PDF / 扫描件 / 表格 | ✗ 跳过非文字块 | 复杂文档再上，或调 RAGFlow 兜底 |
| **分块** | 固定窗口 | 基础 | ✓ FixedChunker | 保留但加标题前缀 |
| | ★ 按结构（标题/章节）分块 | 章节语义完整 | ✗ | **上**：见 §四 |
| | 语义分块 | 语义边界对齐 | ✗ | 可选 |
| | Parent-Child（小块检索、大块给模型） | 召回准 + 上下文足 | ✗ | 进阶 |
| **向量化** | Dense embedding | 语义相似 | ✓ embedding-3 | 保留 |
| | Sparse（BM25/SPLADE） | 关键词/专有名词精准命中 | △ 但中文分词废了 | **修中文分词** |
| **Pre-retrieval** | ★ Query 改写（HyDE/扩写） | 学生口语化提问→专业 query | ✗ | **上**：反思区特别有用 |
| | Query 路由 | 不同问题去不同库 | ✗ | 多库后再上 |
| **Retrieval** | ★ 混合检索（向量+关键词+RRF） | 召回互补 | △ 结构在，中文路废 | **修中文分词** |
| **Post-retrieval** | ★★ Rerank（cross-encoder） | 精排，提准的最大杠杆 | ✗ | **必上**：见 §五 |
| | ★ 相似度阈值 + 去重 | 过滤不相关 | ✗ | **上** |
| | 上下文压缩 | 去冗余 | ✗ | 可选 |
| **Generation** | ★ Citation/溯源 | 可信、可核查 | ✗ | **上**：见 §三 |
| | Self-RAG / CRAG | 模型自查检索质量 | ✗ | 进阶 |

> **关键认知**：在中文教育知识库这种"文档相对规整、问法口语化、要求可信"的场景里，投入产出比最高的三件事是 **①rerank ②标题上下文 ③citation**，而不是换更大的 embedding 模型。

### 2.3 你当前的位置 vs 推荐目标

```
当前：  Naive RAG，且关键词路失效 ≈ 纯向量 Naive RAG
第一步：Advanced RAG（修中文分词 + rerank + 阈值 + 标题上下文 + 清洗）   ← 二阶段主目标
第二步：加 Query 改写 + Citation 完整闭环 + parent-child
第三步：Modular RAG（路由 / Self-RAG），按需
```

---

## 三、溯源（Citation）设计（你问"我还希望进行溯源"）

溯源 = 回答里的每一句论断，都能追回到"哪份文档、第几页、哪一段原文"。分四层做，**缺一层就断**：

### 3.1 存得下：chunk 表 metadata 富化

当前 metadata 只有 `{page, type}`（`document_service.py:170`），且 DOCX/TXT 的 page 恒为 0。补成：

```python
metadata = {
    "doc_id": doc_id,
    "doc_name": doc.doc_name,          # 冗余，避免每次 join
    "file_type": doc.file_type,
    "page": block.get("page"),          # PDF 真实页码
    "heading_path": ["第一章 概述", "1.2 定义"],  # 章节面包屑
    "char_start": 1234,                 # 在文档纯文本中的字符偏移
    "char_end": 1580,
    "chunk_index": i,                   # 文档内顺序
}
```

- `heading_path`：解析时维护一个"标题栈"，遇到 title 就 push/replace，每个 chunk 记录当前栈快照。
- `char_start/char_end`：解析后把整篇文本拼起来，记录每个 chunk 的偏移，可精确高亮定位（解决 DOCX/TXT 没有页码的问题）。

### 3.2 取得出：检索结果透出 metadata

`controller/retrieval_controller.py:60-65` 现在丢掉了 metadata。改成在响应里带上 `doc_name / page / heading_path / chunk_id / score`，前端就能渲染引用卡片。

### 3.3 用得上：prompt 要求模型标注引用

system prompt 里明确约束：

```
你必须基于【参考资料】回答。每一段论断末尾用 [1][2] 标注引用了哪条资料（编号对应参考资料序号）。
若参考资料不足以回答，直接说明，不得编造。回答末尾输出 JSON：{"citations": [{"id":1, "chunk_id": "..."}]}
```

模型输出自带 `[1]`，前端把 `[1]` 渲染成可点击、点击展开原文片段 → 完整溯源闭环。

### 3.4 看得见：前端引用交互

- 正文中 `[1]` → 悬浮显示 doc_name + page + 原文片段
- 点击 → 跳转文档原文（PDF 按 page，DOCX/TXT 按 char_offset 滚动高亮）

> 你现在是四层**全断**（存得不全、取出丢掉、prompt 没要求、前端没展示）。优先做 3.1 + 3.2 + 3.3，3.4 前端后补。

---

## 四、数据清洗：到底清洗什么（你问"数据如何清洗"）

清洗不是一步，是**四个时机**各清一类。当前你一个都没做（`clean_text` 写了没人调）。

### 4.1 清洗的四个时机 × 各清什么

| 时机 | 清什么 | 具体规则 | 落点 |
|---|---|---|---|
| **① 上传前（文件级）** | 水印、页眉页脚、目录页 | PDF 抽取后剔除重复出现的页眉/页脚串；识别并丢弃目录页 | `parser/pdf_parser.py` 增强 |
| **② 解析后（文本级）** | 断行、乱码、HTML、多余空白、繁简 | 行尾无句末标点且下行非标题→合并断行（修 PDF 跨页切断）；去 HTML 标签；`\s+` 归一；可选繁→简 | 新增 `service/cleaner.py`，在 parser 输出后、chunker 前 |
| **③ 分块后（chunk 级）** | 短块、乱码块、重复块 | `len<20 字` 丢弃或并入相邻；中文字符占比过低且非外文→丢弃；精确 hash 去重 + 近似去重（余弦>0.95） | `chunker` 输出后、入库前 |
| **④ 向量化前（校验）** | 超长、空内容 | 超长截断到模型上限；空/纯空白跳过 | `embedding_service.embed_texts` 入口 |

### 4.2 接入位置（改一处就串起来）

在 `document_service.process_document` 里，在 `parser.parse` 之后、`chunker.chunk` 之前插一个清洗阶段：

```
解析 blocks → 【清洗①+②】 → 分块 → 【过滤③】 → 入库 → 【校验④】向量化
```

### 4.3 最小可用清洗规则（先上这套，立竿见影）

```python
# service/cleaner.py（新建）
import re, hashlib

def clean_block_text(text: str) -> str:
    text = re.sub(r'<[^>]+>', '', text)          # 去 HTML
    text = re.sub(r'[ \t]+', ' ', text)          # 归一空白
    text = re.sub(r'\n{3,}', '\n\n', text)       # 多空行归一
    # 合并断行：行尾不是句末标点，且下一行不是空行 → 拼回（修 PDF 跨页切断）
    text = re.sub(r'([^.!?。！？；;])\n([^\n])', r'\1\2', text)
    return text.strip()

def is_valid_chunk(content: str) -> bool:
    if len(content) < 20:                         # 太短
        return False
    chinese = sum(1 for c in content if '一' <= c <= '鿿')
    if 0 < len(content) and chinese / len(content) < 0.15 and not re.search(r'[a-zA-Z]{3,}', content):
        return False                              # 既非中文也非正常外文 → 乱码
    return True

def dedup_key(content: str) -> str:
    return hashlib.md5(content.strip().encode()).hexdigest()
```

然后在 `document_service` 里：清洗每个 block 文本 → chunk → 过滤 `is_valid_chunk` → 用 `dedup_key` 在本文档内去重。

---

## 五、整体调整：按优先级的落地路线

**别一次全改**。按 P0 → P1 → P2 推进，每改一项就用"检索测试页"对比 before/after（同样的 5 个问题，看 top-5 是否更相关）。

### P0 本周必修（修 bug，止损）

1. **修 DOCX 缩进 bug**：`docx_parser.py:50` 把 `return blocks` 移出 `for` 循环（减一级缩进）。修完用一份 .docx 验证能解析出全部段落。
2. **删文档级联删 chunk**：`document_service.delete_documents` 里加一句把对应 `rag_chunk.del_flag='2'`（或物理删）。
3. **关键词路二选一**：
   - 方案 A（推荐，省事）：MVP 阶段先**关掉关键词路**，只走向量检索（你的中文 `simple` 分词本来就没用）。`hybrid_search` 里注释掉 `_keyword_search`。
   - 方案 B（要保留混合检索）：给 PG 装 `zhparser` 或 `pg_jieba` 扩展，`to_tsvector` 改用中文配置；或引入 `rank_bm25` 在内存算 BM25。

### P1 二阶段核心（质量飞跃，这是"不满意"的解药）

4. **上 rerank（最大杠杆）**：
   - 本地：`BAAI/bge-reranker-v2-m3`（中文好、轻量），向量召回 top-20 → rerank 取 top-5。
   - 或 API：硅基流动 / Cohere rerank（省部署）。
   - 落点：`retrieval_service.hybrid_search` 召回后加一步 `_rerank(query, candidates)`。
5. **加相似度阈值**：rerank 后过滤 `score < 0.3` 的（具体阈值按测试集调）。
6. **标题上下文保留**：解析时维护标题栈，chunk 内容前缀加上 `heading_path`（"第一章 概述 > 1.2 定义\n" + 原文）。
7. **接入清洗**：按 §四 在 `process_document` 串入 `cleaner.py`。
8. **打通溯源**：按 §三 做 metadata 富化 + controller 透出 + prompt 约束（这三步一起做才闭环）。
9. **统一 RAG 上下文构建**：新建 `service/rag_context_builder.py`，把情境/决策/反思/批阅 4 处的 prompt 拼接收敛成一处，参数（top_k、截断、引用格式）统一。

### P2 量大了再做（工程优化）

10. Query 改写：反思区把学生反思文本 → LLM 改写成 2-3 个检索 query → 多路召回合并（场景契合度最高）。
11. `ivfflat` → `HNSW` 索引（chunk 过万后）。
12. `chunk_dao.bulk_insert` 改真批量（`session.add_all` + 单次 flush，或 `bulk_insert_mappings`）。
13. chunk 表加 `embedding_model` 版本字段，为以后换模型留路。
14. 上传异步化：`process_document` 丢到后台任务（你规划文档里的 `rag_embedding_task` 表这时候用上）。
15. 分块策略升级：parent-child（小块检索、父块喂模型）/ 按结构分块。

---

## 六、验证方法（怎么知道改好了）

建一个固定的"检索测试集"：找老师要 10-20 个真实学生提问 + 每个问题人工标注的"理想应召回文档/段落"。每次改动后跑一遍，对比：

- **Recall@5**：理想文档是否进了 top-5
- **MRR**：理想文档的排名倒数的平均
- **人工评分**：回答是否引用准确、有无幻觉

没有这个测试集，"调优"就是凭感觉。这是二阶段最该**先建**的东西。

---

## 七、推荐学习资料（你问"有没有类似资料"）

- **综述（建地图）**：Gao et al. 《Retrieval-Augmented Generation for Large Language Models: A Survey》(2024) — 三代范式 + 各模块技术全景，最该读的一篇。
- **进阶范式**：Self-RAG、CRAG（Corrective RAG）论文 — 模型自查检索质量。
- **工程参考**：RAGFlow 源码（你已经在抄，重点重读 `rag/nlp/search.py` 的 rerank、`rag/app/naive.py` 的分块）；LlamaIndex / LangChain 的 RAG 模式文档。
- **模型**：BGE 系列（`bge-large-zh-v1.5` embedding + `bge-reranker-v2-m3` rerank），中文场景的事实标准，可本地部署省 API 费。
- **评测**：RAGAS 框架（自动评 RAG 的 faithfulness、answer relevancy、context recall）。

---

## 八、一句话总结

> 你现在的 RAG 是"骨架完整的 Naive RAG + 3 个致命 bug + 关键词路中文失效"。
> 二阶段**不要推翻重写**，按 **P0 修 bug → P1 上 rerank+标题上下文+清洗+溯源 → P2 工程优化** 推进。
> 其中 **rerank** 是"不满意"的最大解药，**citation（溯源）** 是"可信"的关键，**清洗** 是"召回干净"的前提。
> 先建一个 10-20 题的检索测试集，所有改动都用它量化验证。