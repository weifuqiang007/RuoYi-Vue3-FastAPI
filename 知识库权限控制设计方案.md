# 知识库（RAG）权限控制设计方案

> 分支：`feature/kb-permission-scope`
> 状态：**设计阶段（仅文档与 demo，不改业务代码）**
> 关联接口：`module_rag/controller/knowledge_base_controller.py`、`module_rag/controller/retrieval_controller.py`
> 日期：2026-07-06

---

## 0. 背景与现状

当前知识库接口（`/rag/kb/*`）权限仅做到「**路由级角色校验**」：

```python
dependencies=[
    PreAuthDependency(),
    RoleInterfaceAuthDependency(['admin', 'teacher']),   # 只要是 admin/teacher 就能进
]
```

存在的问题：

1. **`get_list` 返回全部知识库**（`KnowledgeBaseDao.get_list` 只过滤 `del_flag`），任何登录的 teacher 都能看到他人/他校的全部 KB 元数据。
2. **`get_detail` 按 id 直查**，无归属校验，越权读取详情。
3. **`update` / `delete` 无 owner 校验**，任何 teacher 可改/删他人 KB。
4. **`/rag/retrieval/search` 直接接收 `kb_ids` 列表，完全不校验**调用者对该 KB 的访问权 —— 这是**最严重的越权点**：任意 teacher/student 改一下 `kb_ids` 即可读取他人在向量库里的全部切片内容。
5. **学生角色被挡在门外**：检索路由 `RoleInterfaceAuthDependency(['admin', 'teacher'])` 不含 `student`，但按需求「个人级知识库由学生创建」，学生必须能检索自己的 KB。

本方案在不破坏 RuoYi 框架表（`sys_dept`/`sys_user`/`sys_role`）的前提下，引入「**知识库作用域（scope）**」概念，复用既有 edu 组织模型，实现四级可见性控制。

---

## 1. 需求拆解

### 1.1 知识库四级分类

| 级别 | 标识（建议枚举值） | 创建者 | 语义 |
| --- | --- | --- | --- |
| 公共知识库 | `public` | admin | 全平台所有人可见 |
| 学校级知识库 | `school` | 在校教师 | 同校师生可见 |
| 班级级知识库 | `class` | 在校教师 | 本班学生 + 同校教师可见 |
| 个人级知识库 | `personal` | 在校学生 | 仅学生本人、其归属老师、admin 可见 |

> 标识采用字符串枚举（`public`/`school`/`class`/`personal`），可读性优于 `CHAR(1)`；如团队偏好单字符，可映射 `0/1/2/3`。

### 1.2 可见性矩阵（Viewer × KB scope）

| 　查看者　＼　KB | public | school | class | personal |
| --- | :-: | :-: | :-: | :-: |
| **admin** | ✅ | ✅ | ✅ | ✅（全部） |
| **教师（同校）** | ✅ | ✅（本校） | ✅（同校，详见 §1.3-Q1） | ✅ 仅「归属学生」本人 |
| **教师（他校）** | ✅ | ❌ | ❌ | ❌ |
| **学生** | ✅ | ✅（本校） | ✅（本班） | ✅ 仅本人 |

> 「归属老师」定义：该学生所在班级（`edu_student_profile.class_id`）的任课老师（`edu_teacher_class` 中 `class_id` 命中的教师）。

### 1.3 待确认的设计决策（已给出推荐值）

- **Q1 班级级 KB 对教师的可见粒度**：需求原文「学校-班级级别的知识库，可以由在校教师查看这两个」字面意指**同校所有教师**都能看班级级。
  - 推荐：**同校教师均可看班级级**（贴合原文，且教师跨班备课是常见诉求）。
  - 备选（更严）：仅「该班任课老师」可看 → 可作为后续 `class` 的细粒度开关，本期不实现。
- **Q2 学校级/班级级 KB 学生是否可见**：原文只提教师。
  - 推荐：**学生可见本校 school 级、本班 class 级**（否则这两级对学生无意义，与「教学资源共享」语义冲突）。
- **Q3 学校（school）如何识别**：`sys_dept` 无类型字段。
  - 推荐：用 `ancestors` 物化路径走到根（`parent_id=0` 的祖先即为学校），**不改 `sys_dept`**。详见 §2.2。
- **Q4 admin 能创建哪些级别**：推荐 admin 可建任意级别，默认建 `public`。

> 上述 4 项请确认；若与推荐不一致，仅需调整 §3 的判定条件，数据模型不受影响。

---

## 2. 数据模型设计

### 2.1 `rag_knowledge_base` 增列

```sql
ALTER TABLE rag_knowledge_base
  ADD COLUMN kb_scope      VARCHAR(20) NOT NULL DEFAULT 'personal'
    COMMENT '可见范围：public-公共 / school-学校级 / class-班级级 / personal-个人',
  ADD COLUMN scope_dept_id BIGINT NULL
    COMMENT '作用域部门ID：public/personal为NULL；school=学校dept_id；class=班级dept_id',
  ADD COLUMN owner_user_id BIGINT NULL
    COMMENT '所有者用户ID（personal=学生本人；其余=创建者）';

-- 兼容存量数据：旧 KB 默认归为创建者个人级
UPDATE rag_knowledge_base SET kb_scope='personal', owner_user_id=user_id
  WHERE kb_scope IS NULL OR kb_scope='';
```

字段语义对照：

| 字段 | public | school | class | personal |
| --- | --- | --- | --- | --- |
| `kb_scope` | public | school | class | personal |
| `scope_dept_id` | NULL | 学校 dept_id | 班级 dept_id | NULL |
| `owner_user_id` | admin id | 创建教师 id | 创建教师 id | 学生本人 id |
| `user_id`（现有） | 创建者（审计） | 创建者 | 创建者 | 创建者=owner |

> 说明：`owner_user_id` 与现有 `user_id` 在多数场景同值；单独保留 `owner_user_id` 是为了将来「教师代建学生个人 KB」等场景解耦。若确认无此需求，可直接复用 `user_id`，省一列。现有 `dept_id` 保留作创建者所在部门审计，**不参与权限判定**。

### 2.2 「学校」识别策略（不改 sys_dept）

`sys_dept.ancestors` 是物化路径，例如班级 `dept_id=301` 的 `ancestors='0,100,200'`，表示 `学校100 → 院系200 → 班级301`。

**学校 = ancestors 链中 `parent_id=0` 的那个节点**（即 ancestors 里 0 之后的第一段，或 dept 本身 parent_id 即为 0）。

```
school_id_of(dept_id):
    dept = SELECT parent_id, ancestors FROM sys_dept WHERE dept_id=?
    if dept.parent_id == 0:  return dept_id            # 自己就是学校根
    else:                    return first_nonzero(split(dept.ancestors, ','))
```

提供 `OrgResolver` 工具类统一此逻辑（student.class_id、teacher.department_id、任意 scope_dept_id 都走它）。

> 备选（更清晰但动框架表）：给 `sys_dept` 加 `dept_category`（school/college/class）。考虑到 [[framework-extraction-and-multi-platform]] 的「框架表保持纯净」原则，本期**不采用**，留作后续增强。

### 2.3 复用既有 edu 组织关系（无需新建表）

| 关系 | 来源表 | 用途 |
| --- | --- | --- |
| 学生 → 班级 | `edu_student_profile.class_id` | 学生 class 级 / personal 判定 |
| 教师 → 任课班级集合 | `edu_teacher_class`（user_id, class_id） | 教师 class 级 / 归属学生判定 |
| 教师 → 院系/学校 | `edu_teacher_profile.department_id` → ancestors | 教师 school 级判定 |
| 学生 → 学校 | `edu_student_profile.class_id` → ancestors | 学生 school 级判定 |
| 归属学生集合 | `edu_student_profile` WHERE class_id ∈ 教师任课班级 | 教师 personal 级判定 |

---

## 3. 权限判定算法（核心）

### 3.1 ViewerContext（请求级上下文）

每次请求按当前用户构造一次 `KbViewerContext`，后续所有 KB 接口复用：

```python
@dataclass
class KbViewerContext:
    user_id: int
    is_admin: bool
    role: str                 # 'admin' | 'teacher' | 'student'
    school_id: int | None     # 本人所归属的学校 dept_id
    class_id: int | None      # 学生：所在班级；教师：None
    taught_class_ids: list[int]   # 教师：任课班级集合
    owned_student_ids: list[int]  # 教师：其任课班级里的学生 user_id 集合
```

构造逻辑（伪码，放 `KbPermissionService.build_context(current_user)`）：

```
is_admin = (user_id == 1) or ('admin' in role_keys)
role     = pick primary role_key among {admin, teacher, student}
if student:
    class_id  = EduStudentProfile.get(user_id).class_id
    school_id = OrgResolver.school_id_of(class_id)
if teacher:
    taught = EduTeacherClass.list(user_id)           # [class_id, ...]
    school_id = OrgResolver.school_id_of(teacher.department_id 或 user.dept_id)
    owned_students = EduStudentProfile.user_ids_where(class_id IN taught)
```

### 3.2 列表可见性（一条 SQL 完成）

```sql
SELECT kb.* FROM rag_knowledge_base kb
WHERE kb.del_flag = '0' AND (
    -- ① admin 看全部
    :is_admin
    -- ② 公共库人人可见
    OR kb.kb_scope = 'public'
    -- ③ 学校级：同校（学生/教师）
    OR (kb.kb_scope = 'school' AND kb.scope_dept_id = :school_id)
    -- ④ 班级级：学生看本班
    OR (kb.kb_scope = 'class'  AND :is_student AND kb.scope_dept_id = :class_id)
    -- ⑤ 班级级：同校教师（推荐方案，详见 Q1）
    OR (kb.kb_scope = 'class'  AND :is_teacher
        AND school_id_of(kb.scope_dept_id) = :school_id)
    -- ⑥ 个人级：学生本人
    OR (kb.kb_scope = 'personal' AND :is_student AND kb.owner_user_id = :user_id)
    -- ⑦ 个人级：归属老师
    OR (kb.kb_scope = 'personal' AND :is_teacher AND kb.owner_user_id IN (:owned_student_ids))
);
```

> 第⑤行的 `school_id_of(kb.scope_dept_id)` 不能直接写进 SQL（涉及 ancestors 解析）。实现上两种做法：
> - **简单版**：先查「同校的班级 dept_id 集合」（一次性 `WHERE ancestors LIKE '0,school_id,%' OR dept_id=school_id`），再 `kb.scope_dept_id IN (:same_school_class_ids)`。
> - **join 版**：`JOIN sys_dept d ON d.dept_id = kb.scope_dept_id AND (d.dept_id=:school_id OR FIND_IN_SET(:school_id, d.ancestors))`。
>
> 推荐 join 版，一条 SQL、无需预查。

### 3.3 单库访问校验（详情/检索/改删前置）

```
can_access(ctx, kb) → bool：
    if ctx.is_admin: return True
    match kb.kb_scope:
        case 'public':   return True
        case 'school':   return kb.scope_dept_id == ctx.school_id
        case 'class':    if ctx.is_student: return kb.scope_dept_id == ctx.class_id
                         if ctx.is_teacher: return same_school(kb.scope_dept_id, ctx.school_id)
        case 'personal': if ctx.is_student: return kb.owner_user_id == ctx.user_id
                         if ctx.is_teacher: return kb.owner_user_id in ctx.owned_student_ids
    return False
```

`get_detail` / `retrieval/search` / `update` / `delete` 一律先取 kb → `can_access` 不通过则 403（详情/检索）或 403（改删非 owner，admin 除外）。

### 3.4 创建时的作用域校验

| 创建者角色 | 允许的 kb_scope | scope_dept_id 约束 |
| --- | --- | --- |
| admin | 任意（默认 public） | public→NULL；school/class→需为有效 dept |
| teacher | school / class | school→必须==本人 school_id；class→必须 ∈ 本人 taught_class_ids |
| student | personal（唯一） | NULL；owner_user_id = 本人 |

服务层 `create` 时强制按上表校验，前端选错/越权构造请求一律拒绝。

---

## 4. 后端修改逻辑（实现指引，本期不改代码）

文件清单与改动点：

### 4.1 `module_rag/entity/do/knowledge_base_do.py`
- `RagKnowledgeBase` 新增三列：`kb_scope`、`scope_dept_id`、`owner_user_id`（对应 §2.1）。

### 4.2 `module_rag/entity/vo/knowledge_base_vo.py`
- 新增枚举 `KbScope`（public/school/class/personal）。
- `KnowledgeBaseCreateModel` 增 `kb_scope: KbScope = personal`、`scope_dept_id: int | None = None`。
- `KnowledgeBaseResponseModel` 增 `kb_scope`、`scope_dept_id`、`owner_user_id`，便于前端展示「级别」标签。
- 新增 `KbAccessDeniedException` 或复用 `PermissionException`。

### 4.3 `module_rag/dao/knowledge_base_dao.py`
- 新增 `get_visible_list(db, ctx)`：实现 §3.2 的 SQL（join sys_dt 版）。
- 新增 `get_accessible_by_id(db, kb_id, ctx)`：取单条 + 由调用方 `can_access` 判定（或直接在 SQL 里带 ctx 条件）。
- `get_list` 保留但**仅 admin / 内部任务调用**，业务接口改走 `get_visible_list`。

### 4.4 `module_rag/service/knowledge_base_service.py` + 新增 permission 服务
- 新增 `module_rag/service/kb_permission_service.py`：
  - `KbViewerContext` dataclass
  - `build_context(db, current_user) -> KbViewerContext`
  - `can_access(ctx, kb) -> bool`
  - `OrgResolver.school_id_of(db, dept_id)`（可放 `module_learning` 复用）
- `KnowledgeBaseService`：
  - `create`：按 §3.4 校验 scope 合法性，写入 `kb_scope/scope_dept_id/owner_user_id`。
  - `get_list(db, ctx)`：转调 DAO `get_visible_list`。
  - `get_by_id`、`update`、`delete`：先 `get_accessible_by_id`，未授权抛 403；`update/delete` 额外要求 owner 或 admin。

### 4.5 `module_rag/controller/knowledge_base_controller.py`
- 路由依赖放开学生：`RoleInterfaceAuthDependency(['admin', 'teacher', 'student'])`（列表/详情/检索）。
  - 创建接口可按角色再细分：admin/teacher/student 都可建，但能建的 scope 不同（由 service 兜底校验）。
- 每个端点注入 `current_user`，构造 `ctx` 后传给 service：
  - `create`：带 current_user 做 scope 校验。
  - `get_list`：传 ctx。
  - `get_detail`：传 ctx，未授权 403。
  - `update`/`delete`：传 ctx，非 owner 且非 admin → 403。

### 4.6 `module_rag/controller/retrieval_controller.py`（**越权修复，必做**）
- 路由依赖加 `student`。
- `search` 端点：
  1. 注入 `current_user` → 构造 ctx。
  2. 对入参 `data.kb_ids` 逐个 `can_access` 过滤；剔除无权访问的 id。
  3. 若过滤后为空 → 返回空结果（或 403，按产品语义定）。
  4. 只把**有权**的 kb_ids 传给 `RetrievalService.hybrid_search`。

### 4.7 数据迁移
- 执行 §2.1 的 `ALTER` + 存量回填语句。
- 建议给 `kb_scope`、`scope_dept_id`、`owner_user_id` 加索引（`scope_dept_id` + `kb_scope` 联合索引收益最大）。

---

## 5. 前端修改逻辑（实现指引，本期不改代码）

> 前端目录：`ruoyi-fastapi-app/src`。知识库管理页通常在 RAG 相关视图，检索选择器在对话/检索页。

### 5.1 创建知识库表单
- 「可见范围」单选控件，**按当前用户角色动态渲染可选项**：
  - admin：`公共 / 学校级 / 班级级 / 个人级`（默认公共）
  - teacher：`学校级 / 班级级`（默认学校级）
    - 选「学校级」：`scope_dept_id` 自动取本人学校（前端无需选，后端兜底）。
    - 选「班级级」：下拉**仅本人任课班级**（调 `/rag/kb/my-classes` 或复用教师班级接口），提交该 `class_id`。
  - student：固定「个人级」，控件置灰不可改。
- 提交字段新增 `kbScope`、`scopeDeptId`（驼峰，配合后端 `alias_generator=to_camel`）。

### 5.2 知识库列表页
- 列表数据**已由后端按权限过滤**，前端只负责渲染。
- 增加「级别」列/标签：`公共 / 学校 / 班级 / 个人`，可用颜色区分。
- 「编辑/删除」按钮：后端已做 owner 校验；前端可额外按 `ownerUserId === 当前用户 id || isAdmin` 控制按钮显隐（体验优化，非安全依赖）。

### 5.3 检索 / 对话页的「知识库选择器」
- 可选 KB 列表调 `/rag/kb/list`（已过滤），保证用户只能勾选自己有权的 KB。
- 提交检索前 `kb_ids` 仅来自该列表，杜绝前端构造他库 id（后端 §4.6 仍会二次校验）。

### 5.4 角色/接口权限菜单
- 在系统管理的角色-菜单里，确认 `student` 角色拥有 `/rag/kb/list`、`/rag/kb/{id}`、`/rag/retrieval/search` 的接口权限标识（`system:rag:kb:list` 之类，按既有命名）。

---

## 6. Demo（配套可运行示例）

见同目录 `docs/kb-permission-demo/`：

- `schema.sql`：建表 + 四类 KB + 师生组织样例数据。
- `visibility_demo.py`：纯 Python 实现的 `OrgResolver` / `KbViewerContext` / `can_access` / `visible_list`，含 5 个场景断言（admin 全见、同校教师、他校教师拒绝、学生本班、归属老师看个人库）。可直接 `python visibility_demo.py` 运行，无需数据库。

---

## 7. 落地步骤建议（待确认后执行）

1. **确认 §1.3 的 4 个设计决策**（尤其 Q1/Q2）。
2. 数据库执行 §2.1 迁移。
3. 后端按 §4 顺序实现：DO → VO → DAO → permission service → service → controller → retrieval 修复。
4. 前端按 §5 实现：创建表单 → 列表标签 → 检索选择器 → 菜单权限。
5. 单测：以 demo 的 5 个场景为蓝本写 `test_kb_permission.py`（admin/同校教师/他校教师/学生/归属老师）。
6. 回归检索接口越权场景（手动改 kb_id 验证被拦截）。

---

## 8. 安全自检清单

- [ ] `get_list` 已按 ctx 过滤，不再返回全量。
- [ ] `get_detail` 未授权返回 403（非 404 暴露存在性，或统一 404，按团队规范）。
- [ ] `update`/`delete` 非 owner/非 admin 被拒。
- [ ] **`retrieval/search` 对 `kb_ids` 逐个鉴权**（最高优先级）。
- [ ] `create` 的 scope/dept 由后端兜底校验，不信任前端传值。
- [ ] 学生角色可访问自己有权的相关接口。

---

## 9. 框架层 / 业务层 职责划分（架构补遗）

> 动机：本仓库正在把 RuoYi 抽成可复用框架（见 [[framework-extraction-and-multi-platform]]），需服务「反身性」「律师」等多产品。权限逻辑确实每个项目都遇到，但**只有"机制"可复用，"规则"是领域专属**。本节定义切分线与落地节奏。

### 9.1 原则：机制进框架，规则留业务

| 层 | 职责 | 是否领域无关 |
| --- | --- | --- |
| **框架 `common/`** | 可见性「引擎」：Principal 基类、ScopePolicy 抽象、注册表、过滤管道、组织树解析 | ✅ 与业务无关 |
| **业务 `module_rag`/`module_learning`** | 可见性「规则」：学校/班级/师生/归属老师的含义、谁能看谁 | ❌ 教育专属 |

> 红线：**框架里不许出现 `school`/`class`/`teacher`/`student` 等领域词**。一旦写死，律师产品（律所-律师-当事人、团队-成员-客户）就用不上，框架退化成"被复用的业务模块"。

### 9.2 与现有 `DataScopeDependency` 的关系（互补，不取代）

`common/aspect/data_scope.py` 的 `GetDataScope` 是 RuoYi 自带能力：

| 维度 | 现有 DataScope | 本方案 ResourceScope |
| --- | --- | --- |
| 视角 | **角色视角**：viewer 的 `role.data_scope`(1~5) 决定看什么 | **资源视角**：每条资源自声明 `scope_type` |
| 依据 | `sys_dept` 树 + `user_id` | `scope_type` + `scope_ref` + 业务成员关系图 |
| "public 人人可见"档 | ❌ 无 | ✅ 有 |
| 成员图（如归属老师） | ❌ 只认 dept 父子 | ✅ 由业务 Policy 解析 |

→ 二者并列共存：DataScope 管"角色能看哪些行"，ResourceScope 管"这条资源对哪些主体可见"。本方案补的是后者。

### 9.3 框架层新增（`common/aspect/resource_scope.py`，领域无关，≈80 行）

> 命名/风格对齐既有 `GetDataScope` + `DataScopeDependency`、`RequestContext`。

```python
# common/aspect/resource_scope.py  （草图）
from abc import ABC, abstractmethod
from dataclasses import dataclass, field
from typing import Any, Generic, TypeVar

from fastapi import Depends, Request, params
from sqlalchemy import ColumnElement

from common.context import RequestContext

T = TypeVar('T')  # 资源 ORM 模型类型


@dataclass
class Principal:
    """请求级主体身份（领域无关基类）。业务继承后扩展字段/填充 attrs。"""
    user_id: int
    is_admin: bool
    role_keys: list[str] = field(default_factory=list)
    attrs: dict[str, Any] = field(default_factory=dict)  # 业务自定义：school_id/class_id/...


class ScopePolicy(ABC, Generic[T]):
    """资源可见性策略抽象。业务模块为每类资源注册一个实现。"""

    @abstractmethod
    def build_principal(self, current_user) -> Principal:
        """从 CurrentUserModel 构造 Principal（业务在此查组织关系填 attrs）。"""

    @abstractmethod
    def visible_filter(self, principal: Principal, model: type[T]) -> ColumnElement:
        """列表过滤：返回 where 条件（SQL 级，保证可扩展）。"""

    @abstractmethod
    def can_access(self, principal: Principal, resource: T) -> bool:
        """单条访问判定（详情/检索/改删前置）。"""


class ScopeRegistry:
    """按 resource_key 注册/查找 Policy。"""
    _map: dict[str, ScopePolicy] = {}

    @classmethod
    def register(cls, resource_key: str, policy: ScopePolicy) -> None:
        cls._map[resource_key] = policy

    @classmethod
    def get(cls, resource_key: str) -> ScopePolicy:
        if resource_key not in cls._map:
            raise KeyError(f'未注册资源作用域策略: {resource_key}')
        return cls._map[resource_key]


def ViewerDependency(resource_key: str) -> params.Depends:  # noqa: N802
    """注入当前请求的 Principal（按 resource_key 取对应 policy 构造）。"""
    def _resolve(request: Request) -> Principal:
        current_user = RequestContext.get_current_user()
        return ScopeRegistry.get(resource_key).build_principal(current_user)
    return Depends(_resolve)
```

### 9.4 业务层（`module_rag/service/kb_scope_policy.py`，教育专属）

```python
# module_rag/service/kb_scope_policy.py  （草图）
from dataclasses import dataclass, field
from common.aspect.resource_scope import Principal, ScopePolicy, ScopeRegistry
from module_rag.entity.do.knowledge_base_do import RagKnowledgeBase


@dataclass
class KbPrincipal(Principal):
    # 领域专属身份（框架基类不知这些字段的存在）
    school_id: int | None = None
    class_id: int | None = None
    taught_class_ids: list[int] = field(default_factory=list)
    owned_student_ids: list[int] = field(default_factory=list)


class KbScopePolicy(ScopePolicy[RagKnowledgeBase]):
    def build_principal(self, current_user) -> KbPrincipal:
        # 查 edu_student_profile / edu_teacher_class / edu_teacher_profile，
        # 复用既有 find_in_set(dept_id, ancestors) 模式解析 school_id（见 §2.2）。
        ...

    def visible_filter(self, p: KbPrincipal, model):
        # §3.2 的 SQL：public / school=同校 / class=本班或同校(join sys_dept) / personal=本人或归属学生
        ...

    def can_access(self, p: KbPrincipal, kb) -> bool:
        # §3.3 的判定
        ...


# 模块加载时注册（业务自管 key 命名）
ScopeRegistry.register('rag.knowledge_base', KbScopePolicy())
```

Controller 侧用法（与既有 `CurrentUserDependency` 风格一致）：

```python
@knowledge_base_controller.get('/list', summary='获取知识库列表')
async def get_list(
    query_db: Annotated[AsyncSession, DBSessionDependency()],
    principal: Annotated[KbPrincipal, ViewerDependency('rag.knowledge_base')],
):
    policy = ScopeRegistry.get('rag.knowledge_base')
    cond = policy.visible_filter(principal, RagKnowledgeBase)
    kb_list = await KnowledgeBaseDao.get_visible_list(query_db, cond)
    ...
```

### 9.5 哪些做成"配置"，哪些必须写代码

「未来改配置就好」要打折：只有下面两类能数据驱动，**成员关系语义必须写代码注册 Policy**。

| 项 | 可否配置 | 落点 |
| --- | --- | --- |
| 有哪些 scope 类型（public/school/class/personal） | ✅ 配置 | `config/resource_scopes.yaml` |
| 每个角色**能创建**哪种 scope（admin→public…） | ✅ 配置 | 同上 `create_rules` |
| 「归属老师」= 任课班级的学生 | ❌ 代码 | `KbScopePolicy.build_principal/can_access` |
| 学校如何从 dept 解析 | ❌ 代码（复用 ancestors） | `OrgResolver` 工具 |

配置草图：

```yaml
# config/resource_scopes.yaml （只管"有什么/谁能建"，不管"成员怎么判"）
rag.knowledge_base:
  scope_types: [public, school, class, personal]
  create_rules:
    admin:   [public, school, class, personal]
    teacher: [school, class]
    student: [personal]
```

框架读此配置做：① create 时校验 scope 合法性；② 前端下拉项渲染。**成员判定不走配置，走注册的 Policy 代码。** 准确口号是「**注册一个 Policy + 声明 scope 类型**」，不是「纯改配置」。

### 9.6 落地节奏：本次做什么，等律师产品做什么

避免在只有 1 个用例时把抽象焊死（抽象错位风险）。按 Rule of Three 分两步：

**本次（KB 任务）就做：**
1. `module_rag` 按 §4 完整实现，但**从第一天起落在 `KbScopePolicy` 接口后**（面向接口，零额外成本）。
2. `common/aspect/resource_scope.py` 落地 Principal + ScopePolicy ABC + ScopeRegistry + ViewerDependency（≈80 行、零领域词、低风险）。
3. 学校解析复用既有 `find_in_set(dept_id, SysDept.ancestors)` 模式，不新增 util。
→ 这一步让未来抽取变成"搬文件 + 改 key"，零返工。

**等律师产品落地（第二用例）再做：**
1. 若两个产品 Policy 形状一致 → 把 `ScopePolicy`/`ScopeRegistry`/`ViewerDependency` 提升为模板仓一等公民，纳入 [[framework-extraction-and-multi-platform]] 的 `ruoyi-fastapi-template`。
2. 若律师产品也有层级组织 → 抽取通用 `OrgResolver`（基于 `ancestors`）。
3. 视情况把 `Principal.attrs` 升级为强类型扩展点。

> 一句话：**先把"显然通用且低风险"的机制放进 `common/`，把"领域规则"留在 `module_rag`，等第二个产品验证后再固化抽象形态。**
